web: PIDFILE=tmp/pids/server.test.$PORT.pid CYPRESS=1 bundle exec puma -t 1:1 -w 0 -e test -p $APP_PORT
stripe: stripe listen --forward-to http://localhost:$APP_PORT/checkout/webhook ${STRIPE_CONTEXT_ID:+--device-name $STRIPE_CONTEXT_ID}