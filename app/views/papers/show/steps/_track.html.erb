<%# Step 5: Track - Track gift / verify funds %>
<div class="<%= 'hidden' unless local_assigns[:current_step] == 5 %> flex flex-col gap-4"
     data-steps-target="content"
     data-steps-index="5">

  <div class="text-center">
    <h3 class="font-semibold text-base-content">You're All Set! 🎉</h3>
    <p class="text-sm text-base-content/60 mt-1">Your Bitcoin gift is ready. Here's what you can do next:</p>
  </div>

  <%# Track HongBao button %>
  <%= link_to new_saved_hong_bao_path,
        target: "_blank",
        class: "btn btn-primary btn-lg w-full",
        data: {
          controller: "binding",
          action: "bitcoin:changed@window->binding#attribute",
          binding_name_value: "publicAddressText",
          binding_attribute_value: "href",
          binding_template_value: "#{new_saved_hong_bao_path}?address={publicAddressText}"
        } do %>
    <%= heroicon "bookmark", class: "w-5 h-5" %>
    Save & Track This Gift
  <% end %>

  <%# Verify on blockchain %>
  <%= link_to "#",
        target: "_blank",
        class: "btn btn-outline w-full",
        data: {
          controller: "binding",
          action: "bitcoin:changed@window->binding#attribute",
          binding_name_value: "publicAddressText",
          binding_attribute_value: "href",
          binding_template_value: "https://mempool.space/address/{publicAddressText}"
        } do %>
    <%= heroicon "magnifying-glass", class: "w-5 h-5" %>
    Verify on Blockchain
  <% end %>

  <%# Tips %>
  <div class="bg-base-200 rounded-lg p-4 space-y-2 text-sm">
    <p class="flex items-start gap-2">
      <%= heroicon "check-circle", class: "w-5 h-5 text-success shrink-0 mt-0.5" %>
      <span><strong>Print</strong> the PDF and keep it safe</span>
    </p>
    <p class="flex items-start gap-2">
      <%= heroicon "check-circle", class: "w-5 h-5 text-success shrink-0 mt-0.5" %>
      <span><strong>Never share</strong> the private key or recovery phrase</span>
    </p>
    <p class="flex items-start gap-2">
      <%= heroicon "check-circle", class: "w-5 h-5 text-success shrink-0 mt-0.5" %>
      <span><strong>Gift it!</strong> The recipient can claim anytime</span>
    </p>
  </div>
</div>
