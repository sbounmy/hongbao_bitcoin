<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hong₿ao - Getting Started</title>
  <style>
    @page {
      size: 80mm 160mm;
      margin: 0;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, "SF Pro Display", "Helvetica Neue", sans-serif;
      font-size: 8pt;
      line-height: 1.3;
      color: #2c2c2c;
      background: white;
      font-weight: 400;
    }

    .page {
      width: 80mm;
      height: 160mm;
      padding: 5mm;
      display: flex;
      flex-direction: column;
      position: relative;
      background: white;
    }

    /* Header - Ultra Minimal */
    .header {
      margin-bottom: 4mm;
      padding-bottom: 3mm;
      border-bottom: 0.15mm solid #e5e5e5;
      text-align: center;
    }

    .logo {
      font-size: 12pt;
      font-weight: 700;
      color: #1a1a1a;
      letter-spacing: -0.3mm;
    }

    .subtitle {
      font-size: 6.5pt;
      color: #999;
      letter-spacing: 0.5mm;
      text-transform: uppercase;
      margin-top: 0.5mm;
    }

    /* Visual Hero */
    .visual-hero {
      background: #fafafa;
      border: 0.1mm solid #e5e5e5;
      border-radius: 2mm;
      padding: 3mm;
      margin: 2mm 0 4mm 0;
      text-align: center;
      height: 22mm;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .visual-content {
      display: flex;
      align-items: center;
      gap: 2mm;
    }

    .envelope-icon {
      width: 18mm;
      height: 14mm;
      background: white;
      border: 0.15mm solid #999;
      border-radius: 1mm;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16pt;
    }

    .plus {
      font-size: 10pt;
      color: #999;
      margin: 0 1mm;
    }

    .wallet-icon {
      width: 22mm;
      height: 14mm;
      background: white;
      border: 0.15mm solid #999;
      border-radius: 1mm;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14pt;
      color: #2c2c2c;
      font-weight: 500;
    }

    /* Steps */
    .steps {
      display: flex;
      flex-direction: column;
      gap: 4mm;
      margin: 5mm 0;
      padding-left: 2mm;
    }

    .step {
      display: flex;
      align-items: flex-start;
      line-height: 1.4;
    }

    .step-number {
      color: #999;
      font-size: 9pt;
      font-weight: 400;
      min-width: 8mm;
      display: inline-block;
    }

    .step-text {
      flex: 1;
      font-size: 8.5pt;
      color: #2c2c2c;
      padding-left: 2mm;
    }

    .step-text strong {
      font-weight: 600;
      color: #1a1a1a;
    }

    /* Info Box */
    .info-box {
      border-top: 0.15mm solid #e5e5e5;
      border-bottom: 0.15mm solid #e5e5e5;
      padding: 3mm 0;
      margin: 4mm 0;
    }

    .info-title {
      font-size: 7.5pt;
      font-weight: 600;
      color: #1a1a1a;
      margin-bottom: 1mm;
    }

    .info-text {
      font-size: 7pt;
      color: #666;
      line-height: 1.4;
    }

    .info-url {
      font-size: 7.5pt;
      font-weight: 500;
      color: #2c2c2c;
      text-decoration: underline;
    }

    /* Footer */
    .footer {
      margin-top: auto;
      padding-top: 3mm;
      font-size: 6pt;
      color: #999;
      text-align: center;
      border-top: 0.15mm solid #e5e5e5;
    }

    .footer-logo {
      font-size: 7pt;
      font-weight: 600;
      color: #2c2c2c;
      margin-bottom: 0.5mm;
    }

    /* Print optimization */
    @media print {
      body {
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
        color-adjust: exact;
      }
    }
  </style>
</head>
<body>
  <!-- Single Page Instructions for Gifters -->
  <div class="page">
    <!-- Header -->
    <div class="header">
      <div class="logo">Getting Started</div>
      <div class="subtitle">Thank you for choosing Hong₿ao</div>
    </div>

    <!-- Visual Hero -->
    <div class="visual-hero">
      <div class="visual-content">
        <div class="envelope-icon">
          🧧
        </div>
        <span class="plus">+</span>
        <div class="wallet-icon">
          ₿
        </div>
      </div>
    </div>

    <!-- Steps -->
    <div class="steps">
      <div class="step">
        <span class="step-number">1.</span>
        <span class="step-text">
          <strong>Access your account</strong><br>
          <ul style="color: #666; font-size: 7.5pt; margin-left: 2mm;">
            <li>Direct purchase: Sign in at hongbaob.tc</li>
            <li>Amazon/others: Activate at hongbaob.tc/activate</li>
          </ul>
        </span>
      </div>

      <div class="step">
        <span class="step-number">2.</span>
        <span class="step-text">
          <strong>Upload photo</strong> of recipient - AI creates personalized Bitcoin artwork
        </span>
      </div>

      <div class="step">
        <span class="step-number">3.</span>
        <span class="step-text">
          <strong>Fund wallet</strong> with Bitcoin or buy with card via Mt Pelerin
        </span>
      </div>

      <div class="step">
        <span class="step-number">4.</span>
        <span class="step-text">
          <strong>Print & fold</strong> the PDF at home, fold along center line
        </span>
      </div>

      <div class="step">
        <span class="step-number">5.</span>
        <span class="step-text">
          <strong>Insert in envelope</strong> and gift to your loved one
        </span>
      </div>
    </div>

    <!-- Support -->
    <div class="info-box">
      <div class="info-title">SUPPORT</div>
      <div class="info-text">
        Visit <span class="info-url">hongbaob.tc/help</span> or email hello@hongbaob.tc
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <div class="footer-logo">Hong₿ao</div>
      Turn Bitcoin into unforgettable gifts
    </div>
  </div>
</body>
</html>