<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hong₿ao - Getting Started</title>
  <style>
    @page {
      size: A4;
      margin: 5mm;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, "SF Pro Display", "Helvetica Neue", sans-serif;
      font-size: 8pt;
      line-height: 1.3;
      color: #2c2c2c;
      background: white;
      font-weight: 400;
    }

    .page-container {
      width: 210mm;
      height: 297mm;
      padding: 5mm;
      display: flex;
      flex-wrap: wrap;
      gap: 5mm;
      align-content: center;
      justify-content: center;
    }

    .page {
      width: 80mm;
      height: 140mm;
      padding: 5mm;
      display: flex;
      flex-direction: column;
      position: relative;
      background: white;
      border: 0.5px dashed #ccc;
    }

    /* Header - Ultra Minimal */
    .header {
      margin-bottom: 4mm;
      padding-bottom: 3mm;
      border-bottom: 0.15mm solid #e5e5e5;
      text-align: center;
    }

    .logo {
      font-size: 12pt;
      font-weight: 700;
      color: #1a1a1a;
      letter-spacing: -0.3mm;
    }

    .subtitle {
      font-size: 6.5pt;
      color: #999;
      letter-spacing: 0.5mm;
      text-transform: uppercase;
      margin-top: 0.5mm;
    }

    /* Visual Hero */
    .visual-hero {
      background: white;
      margin: 2mm 0 3mm 0;
      text-align: center;
      height: 30mm;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .illustration {
      width: 100%;
      height: 100%;
      object-fit: contain;
      max-height: 30mm;
    }

    /* Steps */
    .steps {
      display: flex;
      flex-direction: column;
      gap: 2mm;
      margin: 3mm 0;
      padding-left: 2mm;
    }

    .step {
      display: flex;
      align-items: flex-start;
      line-height: 1.4;
    }

    .step-number {
      color: #999;
      font-size: 9pt;
      font-weight: 400;
      min-width: 8mm;
      display: inline-block;
    }

    .step-text {
      flex: 1;
      font-size: 8.5pt;
      color: #2c2c2c;
      padding-left: 2mm;
    }

    .step-text strong {
      font-weight: 600;
      color: #1a1a1a;
    }

    .info-url {
      font-size: 6.5pt;
      font-weight: 500;
      color: #2c2c2c;
      text-decoration: underline;
    }

    /* Footer */
    .footer {
      margin-top: auto;
      padding: 1mm 0 1mm 0;
      text-align: center;
      border-top: 0.15mm solid #e5e5e5;
    }

    .footer-title {
      font-size: 7pt;
      font-weight: 600;
      color: #1a1a1a;
      margin-bottom: 0.5mm;
      letter-spacing: 0.3mm;
    }

    .footer-text {
      font-size: 6.5pt;
      color: #666;
    }

    /* Print optimization */
    @media print {
      body {
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
        color-adjust: exact;
      }
    }
  </style>
</head>
<body>
  <div class="page-container">
    <% 4.times do %>
      <!-- Instructions Card -->
      <div class="page">
        <!-- Header -->
        <div class="header">
          <div class="logo">Getting Started</div>
          <div class="subtitle">Thank you for choosing Hong₿ao</div>
        </div>

        <!-- Visual Hero -->
        <div class="visual-hero">
          <%= image_tag "hongbao-illustration.jpg", class: "illustration", alt: "Hong₿ao Bitcoin Gift" %>
        </div>

        <!-- Steps -->
        <div class="steps">
          <div class="step">
            <span class="step-number">1.</span>
            <span class="step-text">
              <strong>Access your account</strong><br>
              <ul style="color: #666; font-size: 7.5pt; margin-left: 2mm;">
                <li>Direct purchase: Sign in at hongbaob.tc</li>
                <li>Amazon/others: Activate at hongbaob.tc/activate</li>
              </ul>
            </span>
          </div>

          <div class="step">
            <span class="step-number">2.</span>
            <span class="step-text">
              <strong>Create paper wallet</strong> with recipient's photo - select style and theme
            </span>
          </div>

          <div class="step">
            <span class="step-number">3.</span>
            <span class="step-text">
              <strong>Fund paper wallet</strong> with credit card or no-KYC: Bitcoin, wire transfer
            </span>
          </div>

          <div class="step">
            <span class="step-number">4.</span>
            <span class="step-text">
              <strong>Print</strong> with a trusted printer - follow the PDF instructions<br>
              <span style="color: #666; font-size: 7pt;">⚠️ Anyone with the PDF can steal the funds</span>
            </span>
          </div>

          <div class="step">
            <span class="step-number">5.</span>
            <span class="step-text">
              <strong>Insert in envelope</strong> and your Bitcoin gift is ready!
            </span>
          </div>
        </div>

        <!-- Footer -->
        <div class="footer">
          <div class="footer-title">SUPPORT</div>
          <div class="footer-text">
            Visit <span class="info-url">hongbaob.tc/help</span> • team@hongbaob.tc
          </div>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>