<div class="min-h-screen">
  <%= render "shared/header" %>
  <section class="max-w-7xl mx-auto flex flex-col items-center gap-8 p-8 justify-center">

    <% if @product %>
      <%= render V3::PricingComponent.new(product: @product, title: false) %>
    <% end %>

  </section>
</div>

