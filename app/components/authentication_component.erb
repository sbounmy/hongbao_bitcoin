<%= render component_for_user %>
<div class="divider">OR</div>

<div class="flex flex-col gap-2">
    <%= render Authentication::GoogleComponent.new %>

    <div class="text-center space-y-2">
        <%= link_to "Forgot password?", new_password_path, class: "link link-primary text-sm", data: { turbo_frame: "_top" } %>
    </div>
</div>
