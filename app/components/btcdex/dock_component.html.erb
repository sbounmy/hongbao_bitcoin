<%# Pokedex-style floating dock - uses DaisyUI dock base class %>
<div class="dock dock-sm md:!hidden bg-[#c73e3e] border-t-4 border-[#2a2a2a]">
  <% items.each do |item| %>
    <%= item %>
  <% end %>
  <%= avatar if avatar? %>
</div>
