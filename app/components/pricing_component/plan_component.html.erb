<div class="relative <%= default ? 'bg-base-300' : '' %> rounded-lg">
  <% if default %>
    <div class="absolute -top-6 left-0">
      <span class="badge badge-primary">Most Popular</span>
    </div>
  <% end %>

  <div class="grid grid-cols-4 items-center text-center p-4 rounded-lg">
    <div class="font-medium"><%= tokens %> ₿ao</div>
    <div class="font-mono">$<%= "%.2f" % (price.to_f / tokens) %></div>
    <div class="font-mono">$<%= price %></div>
    <div>
      <%= form_tag checkout_index_path, method: :post, data: { turbo: false } do %>
        <%= hidden_field_tag :price_id, stripe_price_id %>
        <button type="submit" class="btn btn-sm <%= default ? 'bg-accent hover:bg-accent/80 border-none' : 'btn-outline border-base-200 text-base-content hover:bg-base-500 hover:text-white' %>">
          Select
        </button>
      <% end %>
    </div>
  </div>
</div>