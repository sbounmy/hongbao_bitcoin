production:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day
bitcoin_price_fetch:
  class: BitcoinPriceFetcher
  method: fetch_historical_prices
  schedule: "*/5 * * * *"  # Run every 5 minutes
  description: "Fetch daily Bitcoin prices for USD and EUR"
  queue: default
